# Serverless (Vercel) runtime dependencies for the FastAPI backend.
# Deliberately lean: train-only (pandas), seed-only (faker), and optional
# Gemini (graceful heuristic fallback when absent) are excluded to stay
# under the serverless function size limit. Local Docker uses pyproject.toml.
fastapi>=0.115.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.30
asyncpg>=0.29.0
greenlet>=3.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
razorpay>=1.4.1
numpy>=1.26.0
scikit-learn>=1.5.0
xgboost>=2.1.0,<3.0
langgraph>=0.2.0
httpx>=0.27.0
